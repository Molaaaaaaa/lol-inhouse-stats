# ---- 비밀 ----
# 이름 하나만 막으면 .env.bak / .env copy / .dev.vars 로 새어 나간다.
.env
.env*
!.env.example
.dev.vars
.dev.vars.*
*_local.py
config_local.py
secrets.py
credentials.json
riot_api_key.txt
*credential*
*secret*
*.pem
*.key
*.p12

# ---- 빌드 도구 (2026-09-18 Svelte/Vite 전환) ----
# dist/ 는 **커밋한다** — Cloudflare 가 빌드하지 않고 그대로 서빙한다. 관문이 dist 를 검사한다.
node_modules/
.vite/
coverage/
*.tsbuildinfo
